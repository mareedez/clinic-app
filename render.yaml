services:
  - type: web
    name: clinic-server
    runtime: node
    rootDir: server
    buildCommand: npm install && npm run build
    startCommand: npm run setup && npm start
    envVars:
      - key: DATABASE_URL
        value: postgresql://neondb_owner:npg_MeO7vDU8xfWi@ep-steep-band-ahh09zhe-pooler.c-3.us-east-1.aws.neon.tech/clinic_db?sslmode=require&channel_binding=require
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        generateValue: true
      - key: CORS_ORIGIN
        value: https://clinicflowapp.netlify.app,https://*.netlify.app
      - key: PORT
        value: "4000"
